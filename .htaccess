# 增加POST请求大小限制，支持长内容提交
php_value post_max_size 50M
php_value upload_max_filesize 50M
php_value max_input_time 300
php_value max_execution_time 300
php_value memory_limit 256M

# 关闭错误显示（生产环境），错误记录到日志
php_flag display_errors Off
php_value log_errors On
php_value error_reporting E_ALL

# 启用浏览器缓存 - CSS/JS/字体文件缓存1年
<IfModule mod_expires.c>
    ExpiresActive On
    
    # 字体文件缓存1年
    ExpiresByType font/woff2 "access plus 1 year"
    ExpiresByType font/woff "access plus 1 year"
    ExpiresByType font/ttf "access plus 1 year"
    
    # CSS/JS缓存1年
    ExpiresByType text/css "access plus 1 year"
    ExpiresByType application/javascript "access plus 1 year"
    ExpiresByType text/javascript "access plus 1 year"
    
    # 图片缓存1年
    ExpiresByType image/webp "access plus 1 year"
    ExpiresByType image/jpeg "access plus 1 year"
    ExpiresByType image/png "access plus 1 year"
    ExpiresByType image/gif "access plus 1 year"
    ExpiresByType image/svg+xml "access plus 1 year"
    ExpiresByType image/x-icon "access plus 1 year"
</IfModule>

# 设置Cache-Control头
<IfModule mod_headers.c>
    # 字体文件
    <FilesMatch "\.(woff2|woff|ttf)$">
        Header set Cache-Control "public, max-age=31536000, immutable"
    </FilesMatch>
    
    # CSS/JS文件
    <FilesMatch "\.(css|js)$">
        Header set Cache-Control "public, max-age=31536000, immutable"
    </FilesMatch>
    
    # 图片文件
    <FilesMatch "\.(webp|jpg|jpeg|png|gif|svg|ico)$">
        Header set Cache-Control "public, max-age=31536000, immutable"
    </FilesMatch>
</IfModule>

# WordPress 风格伪静态 - 所有请求交给 index.php 处理
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^index\.php$ - [L]
    # NovaCMS 虚拟路由：即使目录/文件存在也交给 index.php 处理
    RewriteRule ^(blog|page|instant|guestbook|gallery|friend-links|announcement|profile|qq_avatar)(/|$) /index.php [L]
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule . /index.php [L]
</IfModule>
