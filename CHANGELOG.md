# NovaCMS 更新记录

## 2026-07-30 — 后台内容工作台重构

### 后台导航

- 参照内容管理系统的操作习惯重构侧边栏，划分为“内容、系统、工具”三个功能区。
- 文章与智能问答支持可展开子菜单，并完善当前页面高亮、键盘操作和无障碍状态。
- 保留网站配置、用户、隐私与付费、主题、统计、留言、邮件、SEO、日志和备份等现有入口。
- 适配浅色、深色、桌面折叠和移动端抽屉模式。

### 新增内容功能

- **页面管理**：支持页面新建、编辑、发布、草稿、排序、导航标记和固定链接访问。
- **文档中心**：支持文档分类、推荐、正文、附件、搜索、分页和下载次数统计。
- **订阅管理**：支持订阅者新增、筛选、状态管理、来源与运营备注；公开接口不会重新激活已退订或退信地址。
- **智能问答**：新增本地知识库、关键词、分类、优先级、启停设置、匹配阈值和后台问答测试。

### 前台与接口

- 新增独立页面路由：`/page/{slug}`。
- 新增文档列表、详情和附件下载路由：`/docs`、`/docs/{slug}`、`/docs/{slug}/download`。
- 新增页面、文档、订阅和智能问答 REST API。
- 默认主题导航加入文档中心入口。
- Markdown 内容采用原始 HTML 转义、URL 协议过滤和危险节点清理。

### 安全与稳定性

- 新增内容模块采用统一管理员启动流程，在会话开始前配置安全 Cookie，并持续校验管理员角色、封禁和删除状态。
- 新增管理操作 CSRF 校验、Slug 规范化、数字与选项严格验证以及资源级表单消息隔离。
- 数据查询使用原生 PDO 预处理兼容的唯一占位符，并限制搜索、分页和问答候选规模。
- 智能问答改用 Unicode 双字匹配与有界候选预筛选，按需读取最终答案，降低误命中与单次请求开销。
- 访问统计会截断超长路径，避免数据库字段溢出。
- 文档附件会过滤危险地址；外部下载先进入离站提示，并对短时间重复请求去重计数。

### 数据结构

- 新增 `cms_pages`、`cms_documents`、`cms_subscribers`、`cms_ai_qa` 和 `cms_ai_settings` 数据表。
- `blog.sql` 已包含完整的新表结构和智能问答默认设置。

### 验证

- 变更 PHP 文件已通过 AST 语法解析。
- JavaScript 和内联脚本已通过语法检查。
- 后台 CSS 已通过 PostCSS 解析，并完成菜单目标、图标、设计变量与数据表字段一致性检查。
- `git diff --check` 通过。
