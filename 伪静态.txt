# Nginx 伪静态规则 (WordPress 风格)
# 所有请求交给 index.php 处理

location / {
    try_files $uri $uri/ /index.php?$args;
}


#Apache ( .htaccess ):

```
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . /index.php [L]
```